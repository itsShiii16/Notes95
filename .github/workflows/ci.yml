name: Placeholder CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Placeholder
        run: echo "CI will be configured after project dependencies are added."
